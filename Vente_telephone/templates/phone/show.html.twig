{% extends 'base.html.twig' %}

{% block title %}Phone{% endblock %}

{% block body %}
    <h1>Phone</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ phone.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ phone.name }}</td>
            </tr>
            <tr>
                <th>Status</th>
                <td>{{ phone.status }}</td>
            </tr>
            <tr>
                <th>Price</th>
                <td>{{ phone.price }}</td>
            </tr>
            <tr>
                <th>Ecran</th>
                <td>{{ phone.ecran }}</td>
            </tr>
            <tr>
                <th>Stockage</th>
                <td>{{ phone.stockage }}</td>
            </tr>
            <tr>
                <th>Picture</th>
                <td>{{ phone.picture }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ phone.description }}</td>
            </tr>
            <tr>
                <th>ViewCounter</th>
                <td>{{ phone.viewCounter }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('phone_index') }}">back to list</a>

    <a href="{{ path('phone_edit', {'id': phone.id}) }}">edit</a>

    {{ include('phone/_delete_form.html.twig') }}
{% endblock %}
